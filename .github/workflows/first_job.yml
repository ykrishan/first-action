name: first-action
on: workflow_dispatch
jobs: 
 first-job:
  runs-on: ubuntu-latest
  staps:
   - name: Print greeting
     run: echo "Hello world!"
   - name: Print goodbye
     run: echo "Done - goodbye!"
